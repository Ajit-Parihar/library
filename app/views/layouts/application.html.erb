<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Library" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= javascript_include_tag "application", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

<div class="navbar">
  <!-- Left Side Navbar Links -->
  <div class="navbar-left">
    <%= link_to "Home", users_path %>
    <%= link_to "Admin", admin_logIn_path %>
    <%= link_to "Sign Up", users_new_path %>
    <%= link_to "Log In", login_user_path %>
    <%= link_to "Upload Your Own Books", usersbooks_path %>
  </div>

  <!-- Right Side Navbar Links (Profile Image & Log Out) -->
  <div class="navbar-right">
    <% if user_present.present? %>
      <%= link_to "Log Out", user_logOut_path %>

      <div class="profile-image-container">
        <% if user_present.profileImage.attached? %>
          <%= image_tag url_for(user_present.profileImage), alt: "Profile Image", class: "profile-image" %>
        <% else %>
          <p>No profile image uploaded</p>
        <% end %>
      </div>
    <% end %>
  </div>
</div>


    <%= yield %>
  </body>
</html>
